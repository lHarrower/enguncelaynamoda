# AYNAMODA Environment Variables
# Copy this file to .env and fill in your actual values

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# OpenRouter (preferred for text models)
# Sign up at https://openrouter.ai and create an API key
# These are server-side secrets set in Supabase, not exposed in the client.
# OPENROUTER_API_KEY will be configured in Supabase Function secrets, not here.
# Optional app metadata for OpenRouter request headers (server-side):
# OPENROUTER_SITE_URL=https://your-site.example
# OPENROUTER_APP_NAME=AYNAMODA

# Google OAuth Configuration
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_google_web_client_id_here

# Optional: Additional API Keys
# EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_key_here
# EXPO_PUBLIC_SENTRY_DSN=your_sentry_dsn_here 

# AI Proxy
# When true, all OpenAI/HF requests go through the Supabase Edge Function 'ai-proxy'.
# Strongly recommended for production to avoid shipping AI secrets in the client.
EXPO_PUBLIC_USE_AI_PROXY=true